import { PatientFormScreen } from '@/components/patients/PatientFormScreen';

/** Figma — Adicionar Paciente (RF004). */
export default function NewPatientScreen() {
  return <PatientFormScreen mode="create" />;
}
